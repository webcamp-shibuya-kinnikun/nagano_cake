 <% if @genre.errors.any? %>
    <div style="color: red">
      <%= @genre.errors.count %>errors prohibited this obj from being saved:<br>
      <ul>
        <% @genre.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model:@genre, url: admins_genre_path, local: true do |f|  %>
        <h4>ジャンル名</h4>
        <%= f.text_field :name, class:"col-lg-12" %>
        <h4>有効フラグ</h4>
        <%= f.radio_button :is_valid, false %>無効
        <%= f.radio_button :is_valid, true %>有効
        <%= f.submit '更新保存', class:"btn btn-primary col-lg-12" %>

        <% end %>