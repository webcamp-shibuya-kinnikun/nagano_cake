<div class="row">

   <%= render "public/items/search", genres: @genres %>

  <div class="col-xs-9">
    
    <div class="main-visual">
      <div class="main-visual-content">
        <p class="main-visual-text">
          ようこそ、NaganoCakeへ！</br>
          このサイトは、ケーキ販売のECサイトになります。</br>
          会員でない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。</br>
        </p>
      </div>
    </div>
    
    <div>
      <h2>オススメ商品</h2>
        <% @random.each do |item| %>
          <div class="pull-left" style="padding:10px 10px 0 0">
            <%= link_to item_path(item) do %>
              <%= attachment_image_tag(item, :image, size: "150x100", fallback: "no_image.jpg") %>
            <% end %>
            <p><%= link_to item.name, item_path(item) %></p>
            <p>¥<%= item.tax_out_price.to_s(:delimited) %></p>
          </div>
        <% end %>
    </div>
    
    <div class="pull-left"><%= link_to "もっと見る>", items_path %></div>
    
  </div>
</div>