<div class="row">
  <h2>会員の方はこちらからログイン</h2>
  <div class="form-horizontal">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <div class="form-group">
        <%= f.label "メールアドレス", class: "col-sm-3 control-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-sm-9 control-label" %>
      </div>

      <div class="form-group">
        <%= f.label "パスワード", class: "col-sm-3 control-label" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "col-sm-9 control-label" %>
      </div>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <h2>登録がお済みでない方</h2>
  <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。</p>

</div>
